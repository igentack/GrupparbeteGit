@using GitGruppen.Core;
@model VehicleView
@using Gitgruppen.Services;
@inject IVehicleTypeSelectListService service;

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Vehicle</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="MemberId" class="control-label"></label>
                <label asp-for="MemberId" class="control-label">@Model.MemberId</label>
                <span asp-validation-for="MemberId" class="text-danger"></span>
                <input type="hidden" asp-for="@Model.MemberId" />
            </div>
            <div class="form-group">
                <label asp-for="FullName" class="control-label"></label>
                <label asp-for="FullName" class="control-label">@Model.FullName</label>
                <span asp-validation-for="FullName" class="text-danger"></span>
            </div>
            <div class="form-group">
            </div>
            <div class="form-group">
                <label asp-for="LicensePlate" class="control-label"></label>
                <input asp-for="LicensePlate" class="form-control" />
                <span asp-validation-for="LicensePlate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Arrived" class="control-label"></label>
                <input asp-for="Arrived" class="form-control" />
                <span asp-validation-for="Arrived" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="VehicleType" class="control-label"></label>
                <select class="form-select" name="thestringGodforgot" asp-items="@await service.GetVehicleTypesAsync()">
                    <option selected>Choose Type</option>
                
                </select> &nbsp;&nbsp;
                
                
                
                @*   <select asp-for="TypeId" class="form-control">

               @foreach(VehicleType t in Model.VehicleTypes)
                {
                        <option value="@t.Id">@t.Type</option>;
                }   


                 </select>
                @*asp-items="@(new SelectList(Model.VehicleTypes, "Id", "Type"))"*@
                @*asp-for="VehicleView.CountryId"*@
                <span asp-validation-for="VehicleType" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Color" class="control-label"></label>
                <input asp-for="Color" class="form-control" />
                <span asp-validation-for="Color" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Brand" class="control-label"></label>
                <input asp-for="Brand" class="form-control" />
                <span asp-validation-for="Brand" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Model" class="control-label"></label>
                <input asp-for="Model" class="form-control" />
                <span asp-validation-for="Model" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="NumberOfWheels" class="control-label"></label>
                <input asp-for="NumberOfWheels" class="form-control" />
                <span asp-validation-for="NumberOfWheels" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a class="btn btn-outline-dark" asp-controller="Members" asp-action="Details" asp-route-id="@Model.MemberId">Back to member</a>

</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
